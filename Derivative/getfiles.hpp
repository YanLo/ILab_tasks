#ifndef GETFILES
#define GETFILES

#include <stdio.h>
FILE* getInputFile(const char* fileName);
FILE* getOutputFile(const char* fileName);
FILE* getDotFile();
FILE* getInputFile();
FILE* getOutputFile();
FILE* getTexFile();
#endif //GETFILES
